root: True
depends:
  - dep

checkoutDeterministic: True
checkoutScript: |
  echo foo > root.txt

buildScript: |
  cp $1/root.txt .
  cp $2/foo.txt .

packageAuditFiles:
    ROOT: root.txt
    FOO:
        filename: foo.txt
        encoding: base64
packageScript: |
  cp $1/* .
