root: True

scriptLanguage: python

depends:
    - name: python-provider
      use: [interpreters]

packageScriptPython: |
    with open("result.txt", "w") as f:
        f.write(os.environ["CANARY"])
