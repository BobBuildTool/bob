depends:
    - lib2

#checkoutSCM:
#   -
#      scm: git
#      url: ${MIRROR}/tsd.buildenv.git
#      dir: tsd.buildenv
#      branch: feature/kloetzke/unittests

buildTools: [t32]
buildScript: "true"
packageScript: |
    echo "libc" > lib.txt

provideDeps:
    - lib2

