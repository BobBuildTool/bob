depends:
    - lib2

#checkoutSCM:
#   -
#      scm: git
#      url: ${MIRROR}/tsd.buildenv.git
#      dir: tsd.buildenv
#      branch: feature/kloetzke/unittests

buildScript: "true"
packageScript: |
    echo "libh" > lib.txt

provideDeps:
    - lib2

