depends:
    - lib2

#checkoutSCM:
#   -
#      scm: git
#      url: ${MIRROR}/tsd.buildenv.git
#      dir: tsd.buildenv
#      branch: feature/kloetzke/unittests

buildTools: [t42]
buildScript: "true"
packageScript: |
    echo "libd" > lib.txt

provideDeps:
    - lib2

